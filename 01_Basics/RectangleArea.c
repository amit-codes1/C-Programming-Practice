#include <stdio.h>

int main()
{
     int length, breadth;

     printf("Enter Length: ");
     scanf("%d", &length);

     printf("Enter Breadth: ");
     scanf("%d", &breadth);

     printf("Area of the reactangle: %f", length * breadth);
     return 0;
}